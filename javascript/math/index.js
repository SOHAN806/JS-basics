/** @format */

// the math object is a built-in object that provides mathematical functions and constants

// Math.max() method returns the number with the highest value
let y = Math.max(5, 10);
console.log(y);

// Math.min() method returns the number with the lowest value
let z = Math.min(5, 10);
console.log(z);

// Math.PI method returns PI (the ratio of a circle's area to the square of its radius, approximately 3.14)
const b = Math.PI;
console.log(b);

// Math.pow() method returns the value of x to the power of y (xy)
let p = Math.pow(4, 3);
console.log(p);

// Math.random() method returns a random number but it will not return 1
let x = Math.random() * 100;
console.log(k);

// Math.round() method rounds a number to the nearest integer
let j = Math.round(2.5);
console.log(j);

// Math.sqrt() method returns the square root of a number
let l = Math.sqrt(9);
console.log(l);

// Math.trunc() method returns the integer part of a number
let s = Math.trunc(8.76);
console.log(s);
